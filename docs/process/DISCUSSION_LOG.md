# 토의 로그 (Discussion Log)

> 본 문서는 과제 요구사항 중 **"과제 착수 시점에서부터 토의가 시작되었고, 마감일 전까지 지속적·주기적으로 토의가 이루어졌음을 증빙"**하기 위한 일일 로그이다.
> AI(Claude)와의 토의를 중심으로 결정사항·근거·다음 액션을 시간순으로 기록한다.

---

## 2026-05-24 (Day 1, 일) — 프로젝트 착수 ✅ 완료

### 참여
- 개발자: ghwo336 (본인)
- AI 페어: Claude (Cowork 모드)

### 토의 주제 1. SW 선정
- **초안**: "내가 쓸 앱을 만들고 싶다 — 실시간 번역 앱"
- **AI의 좁히기 질문**: 클립보드 / 웹 번역기 / 화면 OCR / 음성 실시간 4가지 옵션 제시
- **결정**: **음성 실시간 번역 + 화자 분리 (영→한)**
- **근거**: 본인이 영어 회의·강의 시 실제 겪는 불편 해결. 기술적 도전 풍부 → 프로세스 적용 사례로 적합.

### 토의 주제 2. AI 도구
- **결정**: Claude (Cowork)
- **근거**: 대화 기록이 그대로 토의 증빙이 됨. 본 과제는 "AI와의 협업 프로세스"를 평가하므로 일관된 AI 사용이 유리.

### 토의 주제 3. 프로세스 모델 선정
- **검토한 모델** (강의록 02 기준): 폭포수 / V / 프로토타이핑 / 나선형 / Unified Process / 애자일
- **AI 추천**: 하이브리드 (Waterfall 단계 골격 + 나선형 반복 구현)
- **근거**:
  1. 폭포수 단계 명세성 → 산출물 명확
  2. 나선형 위험분석 → STT/diarization 같은 기술 리스크 통제
  3. 1인 프로젝트라 애자일 팀워크 측면은 불필요
- **결정**: 하이브리드 채택. PROJECT_CHARTER.md §5에 정식 기재.

### 토의 주제 4. 일정
- **결정**: 2주 (Day 1 ~ Day 14, 2026-05-24 ~ 2026-06-07)
- **마일스톤**: PROJECT_CHARTER.md §6.2 참조
- **핵심 규칙**: 매일 1회 이상 커밋 + 본 로그 갱신 → "막판 몰아치기 의심" 방지 (Risk R6)

### 토의 주제 5. 리포지토리
- **결정**: https://github.com/ghwo336/realtime-voice-translator
- **디렉토리 구조 합의**:
  ```
  docs/process/      ← 프로젝트 관리 산출물 (Charter, 본 로그, 회고)
  docs/requirements/ ← SRS, 유스케이스
  docs/design/       ← 아키텍처, UML, 와이어프레임
  docs/test/         ← 테스트 계획·결과·결함 로그
  src/               ← 소스 코드
  tests/             ← 자동화 테스트
  ```

### 오늘의 산출물
- [x] PROJECT_CHARTER.md v1.0
- [x] DISCUSSION_LOG.md (본 문서) — Day 1 엔트리
- [x] README.md
- [x] .gitignore
- [x] 디렉토리 구조

### 다음 액션 (Day 2)
1. 강의록 04_요구.ppt, 05_요구 모델링.ppt 분석
2. 이해관계자(본인) 자기 인터뷰 — 사용 시나리오 3개 이상 도출
3. SRS v0.1 초안 작성 (기능/비기능 요구사항 분리)
4. **커밋 단위**: 분석본 → 인터뷰 → SRS 초안 (최소 3 커밋)

### 미해결 항목 (Parking Lot)
- [ ] 기술 스택 최종 결정 (Whisper 로컬 vs. 클라우드 STT) — Day 3 요구 확정 후 Day 4 설계 단계에서 결정
- [ ] 데스크탑 앱 프레임워크 (Python+Tkinter / Electron / 웹앱) — 동일

---

## 2026-05-25 (Day 2, 월) — 요구사항 분석 (Phase 2 시작) ✅ 완료

### 토의 주제 1. Day 2 시작 시점의 방향성 검토
- **사용자 제안**: "모듈 구조를 먼저 설계하면 어떨까?"
- **AI 반박**: 소공 원칙(요구 → 설계 순서, 추상화 위계, 추적성)에 따라 모듈 설계는 시기상조. 강의록 02·06 근거.
- **결정**: Charter 일정대로 요구사항 분석을 먼저. 모듈/아키텍처는 Day 4-5로.
- **교훈 (lessons learned 후보)**: **"바이브코딩은 AI에게 시키면 즉시 모듈 코드가 나오기 때문에 요구 단계를 건너뛰고 싶은 유혹이 크다"** — 이것이 본 과제 보고서의 핵심 lessons learned 중 하나가 될 것.

### 토의 주제 2. 요구 추출 방법
- **검토**: 강의록 04 §4.2의 7가지 방법(고객 발표, 문헌 조사, 인터뷰, 설문, 브레인스토밍, 관찰, 프로토타이핑)
- **선택**: 1인 프로젝트라 **자기 인터뷰(self-interview) + 시나리오 기반(5H1W)** 채택
- **근거**: 본인이 발주자이자 사용자이자 개발자. 인터뷰의 변형으로 적용 가능.

### 토의 주제 3. 자기 인터뷰 결과 (구조화 질문 4개)
| 질문 | 답변 |
|---|---|
| 주 사용 상황 | 영어 회의/프로젝트 미팅 |
| 환경 | Mac 데스크탑 (1순위) |
| 지연 vs 정확도 | 균형 — 2~3초 + 높은 정확도 |
| 결과 보존 | 화자별 텍스트 로그 파일 |

### 토의 주제 4. 시나리오 도출
- S-01 실시간 자막 (MVP 핵심) / S-02 로그 저장 (MVP) / S-03 오디오 설정 / S-04 오류 복구
- 강의록 04 §4.3 시나리오 기반 분석 5H1W 양식 그대로 적용

### 토의 주제 5. SRS 작성
- **표준**: IEEE 830 (강의록 04 §4.5 인용)
- **품질 기준 적용**: 원자성/완전성/비모호성/통일성/추적성/우선순위/테스트 가능성 — 강의록 04 §4.3
- **결과**: FR 10건, NFR 8건, 제약 5건, **추적성 매트릭스 양방향 검증 완료** (고아 요구사항 0)

### 오늘의 산출물
- [x] USER_SCENARIOS.md (S-01 ~ S-04)
- [x] SRS.md v0.1 (FR 10 + NFR 8 + 제약 5 + 추적성 매트릭스)
- [x] DISCUSSION_LOG.md Day 2 엔트리
- [x] README.md 진행 현황 표 갱신

### 발견된 리스크 / 변경
- **R2 (화자 분리) 강화**: SRS FR-004에 "Sprint 1 실패 시 에너지 기반 fallback" 명시. 위험 완화 경로 문서화.
- **신규 미해결 항목 4건 (O-01 ~ O-04)**: 기술 스택 결정. Day 4 설계 단계에서 일괄 결정 예정.

### 다음 액션 (Day 3)
1. 강의록 05_요구 모델링 분석 (이미 일부 완료)
2. **유스케이스 다이어그램** — 액터 정의 + UC 5~7개
3. **클래스 다이어그램 초안** — 도메인 객체 후보 (Session, Utterance, Speaker, TranscriptLine 등)
4. **시퀀스 다이어그램 (핵심 1건)** — "실시간 자막 표시" 흐름
5. SRS를 v1.0으로 승급 + Open Issues O-01 ~ O-04 중 가능한 것 해소
6. **커밋 단위**: 유스케이스 → 클래스 → 시퀀스 → SRS v1.0 (최소 3 커밋)

---

## 2026-05-26 (Day 3, 화) — 요구 모델링 (Phase 2 종료) ✅ 완료

### 토의 주제 1. 다이어그램 표현 방식
- **검토**: PNG 이미지(draw.io/PlantUML 렌더) vs **Mermaid 텍스트 다이어그램**
- **결정**: **Mermaid 채택**
- **근거**: (a) GitHub가 .md 안의 Mermaid를 자동 렌더 → 채점자가 도구 없이 즉시 시각화 확인 가능, (b) git diff로 변경점이 보임 (이미지는 안 보임), (c) 텍스트라 형상관리 친화적.

### 토의 주제 2. 유스케이스 도출
- **방법**: 강의록 04 §4.4 슬라이드 25의 5개 질문에 답하는 방식으로 UC 후보 추출
- **결과**: UC-01 ~ UC-07 (7건). 액터 5종 (User + 외부 시스템 4종).
- **관계**: include 2건(UC-01→UC-04, UC-04→UC-05), extend 1건(UC-02→UC-07).

### 토의 주제 3. 클래스 후보 식별 방법
- **검토**: 명사 추출법 vs CRC 카드 vs 도메인 사전
- **결정**: **요구사항 명세서·시나리오의 명사**에서 클래스 후보 추출 (강의록 05 §5.2 모델링 과정 ②)
- **결과**: 11개 클래스 후보 + 4개 후보 탈락(중복/액터/세부 구현)

### 토의 주제 4. 외부 시스템을 `<<interface>>`로 모델링한 이유
- **결정**: STT/Translator/AudioCapture/Diarizer/LogStorage를 모두 인터페이스로
- **근거**: Open Issues O-01 ~ O-04 (기술 스택)가 미정. **인터페이스로 캡슐화**해두면 Day 4의 기술 선택이 클래스 다이어그램을 바꾸지 않음. 강의록 05 §5.3 캡슐화 원칙 적용.
- **교훈 (lessons learned 후보 #2)**: **"바이브코딩에서는 외부 컴포넌트가 자주 교체된다 (AI가 제안하는 라이브러리가 바뀜). 따라서 도메인 모델은 처음부터 인터페이스로 캡슐화하는 것이 유지보수 비용을 크게 낮춘다."**

### 토의 주제 5. 시퀀스 다이어그램 선정
- **검토**: 7개 UC 모두 시퀀스 작성 vs 핵심 1건만
- **결정**: **UC-02 (실시간 자막 한 라인) 1건만 우선 작성**
- **근거**: 가장 복잡하고 NFR-001(지연)·R3(실시간성 리스크)와 직결됨. 다른 UC는 단순 CRUD 흐름이라 명세서로 충분. Day 4 설계에서 필요 시 추가.

### 토의 주제 6. 일관성 점검 (강의록 05 §5.6)
- 시퀀스의 모든 클래스가 클래스 다이어그램에 존재 ✅ (단 UI는 의도적 제외 — Day 5 UI 설계에서 정련)
- UC ↔ FR 4축 추적성 매트릭스 완성 (SRS §6.2)
- 고아 요구사항/UC 0건

### 오늘의 산출물
- [x] USECASES.md (액터 5, UC 7, 다이어그램, 명세, 추적성 매트릭스)
- [x] DOMAIN_MODEL.md (클래스 11, 시퀀스 1건, 일관성 점검)
- [x] SRS v0.1 → **v1.0 승급** (4축 추적성, 완전성 ✅로 갱신)
- [x] DISCUSSION_LOG.md Day 3 엔트리
- [x] README.md Phase 2 완료 표시

### 발견된 리스크 / 변경
- **R3 (실시간성) 구체화**: 시퀀스 다이어그램에 `par` 블록으로 병렬 처리 의도 명시. Day 4 설계에서 비동기 모델 결정 필요.
- **신규 미해결 항목 없음**: Open Issues 4건은 Day 4로 이월.

### 다음 액션 (Day 4)
1. 강의록 06_설계원리, 07_아키텍처와 패턴 분석
2. **아키텍처 다이어그램** — 컴포넌트 + 데이터 흐름 (도메인 모델 정련)
3. **기술 스택 최종 결정** — Open Issues O-01 ~ O-04 일괄 해소
4. **비동기 처리 모델** 결정 (asyncio / threading / multiprocessing)
5. SDD v0.1 시작

---

## 2026-05-27 (Day 4, 수) — 설계 (Phase 3 시작) ✅ 완료

### 토의 주제 1. 아키텍처 스타일 선정

어제 만든 도메인 모델(`DOMAIN_MODEL.md`)을 다시 보니 데이터 흐름이 명확한 **단방향 파이프라인**이었다.

```
오디오 → 발화 분리 → 화자 분리 → STT → 번역 → UI
```

이 흐름을 강의록 07 §7.2의 7개 아키텍처 스타일과 매칭해봤다.

| 스타일 | 본 프로젝트 적합도 판단 |
|---|---|
| 클라이언트-서버 | ❌ 단일 데스크탑 앱, 서버 없음 |
| 계층형 | △ UI/Domain/Infra 계층 분리 정도만 |
| 이벤트 기반 | ◯ 오디오 청크 = 이벤트, 잘 맞음 |
| **파이프-필터** | ◎ **단방향 변환 흐름과 정확히 일치** |
| MVC | ◯ UI 분리에 적합 |
| 데이터 중심 / P2P | ❌ 부적합 |

코어 처리는 파이프-필터가 자명했다. 강의록 07 슬라이드 10에 "단점: 자원의 낭비"가 있어 잠시 걸렸지만, 본 프로젝트의 청크 단위가 100ms로 작고 메모리 압박이 크지 않다는 점에서 단점이 미미하다고 판단.

UI 분리는 강의록 §MVC "사용자 인터페이스로부터 비즈니스 로직과 데이터를 분리"가 본 프로젝트의 NFR-008(프라이버시) — 코어가 외부 호출을 격리한다 — 와 정확히 맞는다.

**결론**: **파이프-필터(코어) + 이벤트 기반(컴포넌트 통신) + MVC(UI) 하이브리드**.
**근거**: NFR-001(지연 ≤ 3초)의 핵심은 병렬성. 파이프-필터의 강의록 명시 장점("단순성/병렬성/재사용")이 이 요구와 직결됨.

### 토의 주제 2. 기술 스택 — Open Issues O-01 ~ O-04 일괄 결정

본 프로젝트의 제약을 다시 정리:
- Mac (Apple Silicon) 환경
- 1인 / 14일
- API 예산 ≤ ₩10,000
- 회의 내용 = 민감 (프라이버시 중요, NFR-008)
- Charter C-04: "Claude만 사용"

이 제약 위에서 각 Open Issue를 결정했다.

| ID | 결정 | 결정 근거 |
|---|---|---|
| **O-01 STT** | `faster-whisper` (로컬, medium/large) | Apple Silicon Metal 가속 가능 → 비용 0, 오프라인, NFR-008 프라이버시 충족. `faster-whisper`는 CTranslate2 기반으로 표준 Whisper보다 4-8배 빠름 |
| **O-02 번역** | Claude API (haiku-4-5) | Charter C-04와 정합. LLM이 회의 맥락 이해에서 traditional MT(OPUS-MT/M2M100)보다 우수. haiku-4-5는 비용·속도 균형 |
| **O-03 화자분리** | pyannote.audio 3.1 + 에너지 기반 fallback | Risk R2에서 이미 fallback 경로 식별해둠. 정확도 우선 pyannote, 실패 시 단순 에너지 기반 |
| **O-04 UI** | PySide6 | Mac 네이티브 룩, 무엇보다 **Qt의 signal/slot이 강의록 07 §옵서버 패턴 그대로** → 패턴 구현이 자연스러움 |

→ SRS §8 Open Issues 4건 모두 closed 처리.

### 토의 주제 3. 동시성 모델 — NFR-001로부터 역산

NFR-001(지연 ≤ 3초)을 충족하려면 파이프라인 각 단계가 **순차적으로 막혀선 안 됨**. 동시성 필수.

후보 정리 + 평가:

| 모델 | 평가 |
|---|---|
| `threading` | 콜백 헬, 디버깅 난해. ❌ |
| `multiprocessing` | IPC 비용, Qt GUI 통합 어려움. ❌ |
| `asyncio` 단독 | I/O 친화, 그러나 CPU bound 작업(Whisper) 처리 까다로움 |
| **`asyncio` + `run_in_executor()`** | ✅ I/O는 코루틴, CPU bound는 executor 격리 |

**결정**: **`asyncio` 메인 루프 + `run_in_executor()`로 Whisper 격리**, PySide6와는 `qasync`로 통합.

**백프레셔 설계**: 큐 무한 확장 방지 위해 `asyncio.Queue(maxsize=N)` 사용. STT가 못 따라가면 producer가 자연스럽게 `await`로 막힘 → 자동 백프레셔.

### 토의 주제 4. 디자인 패턴 적용

도메인 모델(인터페이스 5개, 상태 enum, 이벤트 기반 통신)을 강의록 07 §7.3 패턴 카탈로그와 매칭.

| 모델 요소 | 적용 패턴 | 강의록 |
|---|---|---|
| `STTEngine`/`Translator`/`Diarizer` 인터페이스 + 구체 구현 교체 | **전략(Strategy)** | 07 §7.3 |
| 엔진 인스턴스 생성 비용 큼, 한 곳에서 결정 | **팩토리 메소드** | 07 §7.3 |
| OS/SDK 인터페이스를 도메인 인터페이스에 맞춤 | **어댑터** | 07 §7.3 |
| `SessionState` enum + 상태별 허용 동작 다름 | **상태(State)** | 07 §7.3 |
| `Session.lineAdded` → 다중 구독자(UI, Storage) | **옵서버** | 07 §7.3 |
| 모델 로딩 비용 큰 객체 단일 인스턴스 | **싱글톤** | 07 §7.3 |

6개 패턴 자연 매칭. 모두 SDD §5에 정식 기재.

**lessons learned 후보**: 바이브코딩에서 디자인 패턴을 SDD에 명시해두면 후속 코드 생성 일관성이 크게 향상된다 — 패턴 이름 자체가 의사소통의 공용어 역할.

### 토의 주제 5. SOLID 적용 점검 (강의록 06 §6.4)

| 원리 | 본 설계에서의 만족 |
|---|---|
| SRP | 각 filter 모듈이 단계 하나만 — Capture/Segment/Diarize/STT/Translate/Assemble |
| OCP | 새 STT 엔진 추가 시 `STTEngine` 구현체만 추가, 코어 무수정 |
| LSP | 인터페이스 계약(`transcribe(utt) → str`)을 모든 구현체가 동일하게 만족 |
| ISP | 외부 시스템마다 인터페이스 분리 (STT/Translator/Diarizer/LogStorage 별도) |
| DIP | `Session`이 구체 라이브러리(`faster_whisper`) 직접 import 없이 `STTEngine` 추상에 의존 |

DIP가 가장 핵심. 향후 라이브러리 교체 시 `core/` 한 줄도 안 바뀜 — Open Issues가 다시 열리더라도 코어 재작성 없이 대응 가능.

### 토의 주제 6. 응집도·결합도 자기 평가 (강의록 06 §6.3)

| 모듈 | 응집도 | 결합도 |
|---|---|---|
| `core/session.py` | 기능적 (세션 책임 단일) | 데이터 (DTO만 전달) |
| `core/stt_engine.py` | 기능적 | 데이터 |
| `core/translator.py` | 기능적 | 데이터 |
| `ui/main_view.py` | 교환적 (UI 요소 그룹) | 데이터 (signal/slot DTO) |

전 모듈이 **기능적 응집 + 데이터 결합** (강의록 06 최적 등급) 달성.

추가 규칙: `core/`에서 PySide6 import 금지 — 코딩 표준(강의록 09)에서 정식화 예정. 이로써 코어와 UI의 의존성 역전을 코드 레벨에서 강제.

### 오늘의 산출물
- [x] SDD v0.1 (9개 섹션 — 아키텍처/모듈/동시성/패턴/스택/할당/추적성)
- [x] SRS Open Issues 4건 closed
- [x] DISCUSSION_LOG.md Day 4 엔트리
- [x] README.md Phase 3 상태 + §5 기술 스택 섹션 추가

### 발견된 리스크 / 변경
- **R2 fallback 경로 SDD §6에 명시**: pyannote 실패 시 에너지 기반 단순 분리로 자동 전환.
- **신규 상태 DEGRADED**: 컴포넌트 3회 연속 실패 시. 상태 다이어그램 보강 → Day 5에서.

### 다음 액션 (Day 5)
1. 강의록 08_UI 설계 분석
2. UI 와이어프레임 + 컴포넌트 명세 (MainView, SubtitleArea, LevelMeter, Toolbar)
3. 사용자 인터랙션 상태도 (DEGRADED 포함)
4. SDD v0.1 → v1.0 승급

---

<!-- 이후 일자는 매일 아래 템플릿으로 추가 -->

## 템플릿 (복사해서 사용)

```
## YYYY-MM-DD (Day N, 요일) — <단계명>

### 토의 주제
- 

### 결정 사항
- 

### 근거
- 

### 오늘의 산출물
- [ ] 

### 발견된 리스크 / 변경
- 

### 다음 액션
- 
```
